// src/app/auth/login/layout.tsx
// Layout de la pantalla de login — sin sidebar ni topbar

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
